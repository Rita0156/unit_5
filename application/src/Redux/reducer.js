
const initState={
    musicRecords:[],
    isLoading:false,
    isError:false
}

const reducer=( oldState=initState,action)=>{
       const {type,payLoad}=action
       switch (type) {
        case value:
            
            break;
       
        default:
            return oldState
       }
}